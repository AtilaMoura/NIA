# Core FastAPI
# fastapi 0.115+ pra ser compatível com anyio>=4.8 exigido pelo google-genai
# (0.104.1 antigo travava anyio<4, gerando conflito de dependências no build)
fastapi==0.115.6
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
pydantic==2.5.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23

# Autenticação e Segurança
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6

# HTTP Client para chamadas de API
# 0.28.1+ exigido pelo google-genai (0.25.2 antigo conflitava)
httpx==0.28.1

# IA - Google Gemini (migrado 2026-08-26: google-generativeai==0.3.2 está
# descontinuado — repo virou "deprecated-generative-ai-python" — e dava 504
# Deadline Exceeded sempre em ~60s com gemini-2.5-flash, independente do
# tamanho do prompt (ver curso de obreiro/anotação para IA.md). SDK novo
# unificado, mesma API key.
google-genai==2.20.0

# IA - Groq (usa API compatível com OpenAI)
# Já funciona com httpx, não precisa lib específica

# Renderizador de conteúdo (Fase 1 - Estudo IA)
jinja2==3.1.2
