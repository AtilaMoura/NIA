// Usuário fixo pra testar o fluxo do aluno sem login de verdade (Fase 0 ainda não existe).
// user_id=1 já existe no banco (teste-fase7@nia.local), com progresso real no curso 4.
// Trocar por sessão real assim que a Fase 0 (autenticação) for implementada.
export const MOCK_USER_ID = 1;
